# How to set up this project in local environment

- First clone this repo in your local environment and cd into this repo.
- Run `composer setup` to install composer dependencies, copy .env.example to .env file, generate APP_KEY env variable, run `php artisan migrate` to migrate database and lastly install npm dependencies and build static assets. 
